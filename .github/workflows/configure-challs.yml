name: Configure challs

on:
  push:
    paths:
      - "challenges/**"
  workflow_dispatch:

jobs:
  configure_challenges:
    name: Configure challenges
    uses: ctfpilot/challenge-ci/.github/workflows/configure-challs.yml@v1.0.1
    permissions:
      contents: write
      packages: write
      id-token: write
    with:
      runs-on: "ubuntu-latest"
      toolkit-path: "./challenge-toolkit/"
